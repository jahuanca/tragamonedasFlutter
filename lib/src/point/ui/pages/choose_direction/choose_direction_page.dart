
import 'package:flutter/material.dart';

class ChooseDirectionPage extends StatelessWidget {
  const ChooseDirectionPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text('Center'),
    );
  }
}