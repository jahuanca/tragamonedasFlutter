import 'package:flutter/material.dart';
import 'package:utils/utils.dart';

final BoxDecoration decorationAllItemConstant = BoxDecoration(
    border: Border.all(color: primaryColor()),
    borderRadius: BorderRadius.circular(borderRadius()));

const EdgeInsets paddingAllItemConstant = EdgeInsets.symmetric(vertical: 8, horizontal: 16);
const double cardElevationItemConstant = 5;
